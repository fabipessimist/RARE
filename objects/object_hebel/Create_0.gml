/// @description active
// why, what, how?
active = false; //if object is active
connected = object_testtry_lever; // the connected instances
in_range = false;

interaction_radius = 100;

image_speed = 0;

prompt_dist=35;