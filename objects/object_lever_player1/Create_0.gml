/// @description active
// why, what, how?
active = false; //if object is active
connected = object_validationbulb_player1; // the connected instances
in_range = false;

interaction_radius = 75;

image_speed = 0;

prompt_dist=35;